# VTA Compare Releases

This public repository hosts VTA Compare release artifacts and the updater manifest.

The release script publishes installers to GitHub Releases and updates `latest.json` on `main`.
